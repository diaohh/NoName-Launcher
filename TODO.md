# TODO — NoNameLauncher

Pendientes ordenados por prioridad. Los puntos marcados con 🔍 salieron de la auditoria del 14/08/2026; el resto son posteriores.

## P1 — Seguridad y robustez

- [x] 🔍 **Cifrar los tokens en disco** — resuelto. `accessToken`, `microsoft.access_token` y `microsoft.refresh_token` se guardan como un unico blob `secrets` cifrado con `safeStorage`; el resto de la cuenta (username, uuid, expiraciones) sigue en claro para que `config.json` se pueda seguir inspeccionando
  - El fichero lleva ahora un sello `CONFIG_VERSION` y los `config.json` v0 se migran y se cifran en el primer `load()`
  - **Prerrequisito, tambien resuelto**: `load()` ya no sobrescribe el config con los defaults. Un JSON ilegible se mueve a `config.json.corrupt-<timestamp>` y unos secretos indescifrables solo purgan la sesion (`purgeSession`), conservando ajustes, directorio de datos y modpack seleccionado
  - La fuga por IPC era peor de lo anotado: `config:load` devolvia el config **entero** con los tres tokens y estaba expuesto en el preload, pero no en `ipcClient` — canal muerto por la regla de las 4 capas. Eliminado de las tres capas donde existia
  - Si `safeStorage.isEncryptionAvailable()` es `false` (Linux sin keyring) **no se degrada a texto plano**: la sesion vive solo en memoria y al reiniciar se vuelve al login
- [x] 🔍 **No desloguear ante fallos transitorios** — resuelto. helios-core no lanza ante un fallo de red: devuelve `responseStatus: ERROR` con el `RequestError` de got adjunto, y `AuthManager.classifyRestError` lo usa para separar `AUTH_NETWORK` (transporte, 5xx, 429) de `AUTH_INVALID_GRANT`. Solo un codigo terminal borra la cuenta; un error desconocido **tampoco** la borra. `validateSelectedMicrosoftAccount` devuelve `{ ok, code, message }` en vez de un booleano
  - Encontrado de paso y arreglado: `monitorTokenExpiration` comparaba `microsoft.expires_at`, que es la expiracion del **access token** de Microsoft (~1 h) y no la del refresh token (~90 dias), asi que el `setInterval` **deslogueaba a cualquier usuario ~24 h despues de entrar** con un refresh token perfectamente valido. Ahora reintenta el refresco y solo notifica la expiracion si falla por un motivo terminal
- [x] 🔍 **No exponer el accessToken de Minecraft al renderer** — resuelto, `auth:login` ya no lo devuelve. Nada en el renderer lo leia; `LaunchManager` lo saca de `ConfigManager` en el propio main
- [x] 🔍 **Endurecer la ventana OAuth** — resuelto: escucha `did-redirect-navigation` ademas de `did-navigate`, maneja `error=access_denied` (antes la ventana se quedaba abierta para siempre), restringe `will-navigate` a los hosts de Microsoft, deniega popups y corre con `sandbox: true`. Corregido tambien el manejador `closed`, que no distinguia un cierre del usuario de uno provocado por el exito
- [x] 🔍 **CSP y `sandbox: true`** — resuelto. La politica se aplica con `session.webRequest.onHeadersReceived` y **no** con un `<meta>` en `index.html`: el mismo documento lo sirve vite en `pnpm dev`, que necesita `ws:` y `unsafe-eval`, asi que un meta estatico obligaria a aflojarla tambien en produccion. Se marca solo el documento del launcher, nunca las paginas de Microsoft, que traen la suya. La ventana principal pasa a `sandbox: true` (el preload compila a CJS y solo usa `contextBridge`/`ipcRenderer`) y tiene `setWindowOpenHandler` mas un guard de `will-navigate`
  - **Verificado el 01/09/2026** que `onHeadersReceived` **si** intercepta `file://`: en una ejecucion de produccion pasan por el la propia `index.html`, su JS, su CSS y los woff2, todos marcados como documento del launcher. La politica se aplica igual en produccion que en dev, y `'self'` casa con los assets locales. El aviso de Electron sobre `unsafe-eval` que se ve en `pnpm dev` es solo la rama dev de la politica y desaparece empaquetado — no hay que tocar nada
- [ ] 🟡 **Reforzar `usersAllowed` en Firestore Rules — STAND-BY, bloqueado** — no es implementable a dia de hoy. `firebase.js` solo hace `initializeApp` + `getFirestore`: **el launcher no autentica contra Firebase**, asi que en las reglas `request.auth` es siempre `null` y no existe ninguna identidad por la que filtrar `usersAllowed`. El filtrado sigue siendo client-side y cualquiera con el bundle puede leer el catalogo completo
  - **Via A — Cloud Function + custom token.** Una Function valida el accessToken de Minecraft contra `api.minecraftservices.com/minecraft/profile` y emite un custom token de Firebase con el username como claim; el renderer hace `signInWithCustomToken` y la regla queda `allow read: if resource.data.usersAllowed.size() == 0 || request.auth.token.mcUsername in resource.data.usersAllowed`. Es la solucion real, pero exige **plan Blaze** (Spark no permite salida de red a servicios no-Google) e infraestructura nueva que mantener
  - **Via B — rediseñar el dato.** Las reglas permiten leer solo documentos con `isPublic == true` y los packs privados dejan de ser "publicos pero ocultos": pasan a no ser legibles sin identidad. Cierra la fuga sin backend, pero cambia el modelo de `usersAllowed`
  - Mientras siga en stand-by, `usersAllowed` es **solo UX** y no debe presentarse como control de acceso

## P2 — Bloquean una release

- [ ] 🟡 **Falta `resources/icon.png` — STAND-BY** — referenciado por `electron-builder.yml` (win/linux/mac) y por `main/index.js:88`; la carpeta sigue vacia. Es **el unico bloqueante que queda para `pnpm package`**: todo lo demas del P2 esta cerrado. En espera de que se defina el icono
- [x] 🔍 **`extraResources: distribution.json` apunta a un archivo inexistente** — resuelto, entrada eliminada de `electron-builder.yml`
- [x] 🔍 **Version unica** — resuelto. `package.json` pasa a **0.3.0** y es la unica fuente: el main lee `app.getName()`/`app.getVersion()` (que resuelven a `productName` y `version`), y el renderer recibe `__APP_VERSION__`, inyectado en tiempo de build por `electron.vite.config.mjs` porque va en sandbox y no puede leer el fichero. Desaparecen el `launcher_version: '1.0.0'` de `LaunchManager` y el "Build 0.2.1-IND" de `LoginSection`
- [x] 🔍 **Borrar `package-lock.json`** — resuelto, estaba trackeado
- [x] **El usuario ve "undefined" cuando falla el login con Microsoft** — resuelto junto con el P1 de fallos transitorios: era el mismo codigo. `microsoftErrorDisplayable` devolvia objetos planos `{title, desc}`, que no sobreviven a `ipcMain.handle`; ahora es `microsoftError`, una fabrica de `Error` reales con `code` y mensaje en español. Queda pendiente en P3 quitar el prefijo `Error invoking remote method '...'` que Electron antepone
- [x] **El ajuste "Directorio de datos" no tiene efecto** — resuelto separando dos conceptos que estaban colapsados en uno:
  - `getLauncherDirectory()` es la **raiz fija de la app** y no se puede mover: `config.json` vive ahi y `load()` necesita la ruta antes de que exista ninguna preferencia que leer. Intentar mover tambien el config era el problema del huevo y la gallina que bloqueaba este item
  - `getDataDirectory()` es lo que gobierna el ajuste, y de ella cuelga todo lo pesado: `common/`, `instances/`, `manifests/` y los JVM que helios descarga en `<dataDir>/runtime/<arch>`
  - El default pasa de ser la ruta absoluta a `null` ("donde viva el launcher"), asi que el config deja de llevar dentro el `%APPDATA%` del dia en que se creo. Un `config.json` existente conserva su ruta absoluta y resuelve al mismo sitio
  - Una ruta guardada que ahora mismo no sirve (disco externo desconectado, permisos) **cae al default con un warn y no se reescribe** — el disco puede volver. Al elegirla en la UI si se valida con una prueba de escritura y se rechaza en el momento
  - No se puede cambiar con el juego abierto: mover la raiz debajo de un proceso con ficheros abiertos en la instancia deja el estado partido
  - `config:getSettings` expone tambien `defaultDataDirectory`, que es lo que necesitaba el boton "Restablecer" de P4

## P3 — Limpieza de codigo

- [x] 🔍 **Errores por IPC** — resuelto. El sobre `{ ok, code, message }` viaja por el cable y `ipcClient` lo vuelve a convertir en un `Error` real con `.code`, asi que los `try/catch` que ya habia siguen valiendo y no hubo que reescribir cada consumidor
  - `main/ipc/result.js` expone un `handle()` que envuelve `ipcMain.handle`; **todos** los canales pasan por el, para no tener dos contratos conviviendo. Los codigos viven en `src/shared/errorCodes.js`, importado por los dos procesos — comparar contra una copia literal en el renderer es justo el fallo que tenia `PlayButton`
  - `PlayButton` ya no compara substrings: usa `isTerminalAuthCode(err.code)`, asi que un fallo de red deja de poder parecerse a una sesion muerta. Con eso se pudo traducir el `'Session expired. Please login again.'`
  - Desaparece de paso el prefijo `Error invoking remote method '...'` en **todos** los canales, no solo en el de lanzamiento
  - **Encontrado de paso**: `config:save` existia en `channels.js`, en el handler y en el preload pero no en `ipcClient` — canal muerto por la regla de las 4 capas, igual que el `config:load` que se borro en el P1. Eliminado. Y `LoginSection` seguia leyendo `err.desc`, propiedad que dejo de existir cuando el P1 sustituyo `microsoftErrorDisplayable` por `microsoftError`
- [x] 🔍 **Eliminar funciones muertas** — `msftAuth.createMsftLogoutWindow()` borrada al endurecer la ventana OAuth
- [x] 🔍 **Unificar el formato de progreso** — resuelto. Toda la cadena pasa un unico objeto `{ current, total, message }`. Los managers de abajo no saben a que fase del lanzamiento sirven, asi que no inventan `type`: lo añade `LaunchManager`, que es la unica capa que lo sabe. `MinecraftDownloadManager` sigue llevando su sub-fase propia en `phase`
- [x] **Barra de progreso atascada en "Validando archivos"** — resuelto con el mapa fase→porcentaje que el propio item proponia como plan B (`renderer/services/launchPhases.js`): cada fase ocupa un tramo fijo de la barra y la fraccion interna se mapea dentro de el, mas un tope monotono en `LaunchContext` para que nunca retroceda. Una fase que no puede contar nada se queda en su suelo, que sigue siendo avanzar
  - **Encontrado al dibujar el mapa**: `download` se emitia **dos veces** en el flujo, para los assets de vanilla y otra vez para las librerias del loader. Con tramos fijos eso hace que la barra vuelva atras, asi que las librerias pasan a tener su propio `download_libraries`. Regla nueva: un `type` aparece exactamente una vez en el flujo
  - No hizo falta tocar el hashing: como decia el item, la migracion a CDN ya se habia llevado el atasco del MD5 por modulo
- [x] **Errores en ingles visibles al usuario** — resuelto. Quedaban menos de los anotados: la auto-descarga de Java desactivada, el manifiesto de version ausente, "No server selected" y el codigo de salida del instalador de Forge
  - Los de helios-core ya no pasan crudos: `MinecraftDownloadManager` (`MojangIndexProcessor`) y `ensureJava` (`JavaGuard`) los envuelven con mensaje en español y `code`, guardando el original en `cause` y en el log
- [x] 🔍 **`StatusMessage` usa una variable global mutable** — resuelto con `StatusContext` (`useStatus()`), siguiendo el patron de los otros providers
  - **Encontrado de paso**: habia **dos** `<StatusMessage />` montados en ramas distintas del arbol, y `SettingsScreen` no montaba ninguno, asi que no podia enseñar ni un error — justo lo que necesitaba el ajuste del directorio de datos. Ahora hay un unico host en `App.jsx`, por encima de todas las pantallas, asi que un mensaje lanzado al salir de una pantalla sigue visible en la siguiente
- [x] 🔍 **Separar `fullMicrosoftAuthFlow`** — resuelto. Queda partido en `exchangeAuthCode` / `exchangeRefreshToken` (obtienen el juego de tokens de Microsoft) y `resolveMinecraftSession(msAccessToken)` (XBL → XSTS → token MC → perfil). Los tres llamantes los componen y `AUTH_MODE` desaparece; `refreshMCToken` es literalmente "solo la segunda mitad", que es lo que el flag intentaba decir
- [x] 🔍 **Warning de `postcss.config.js` en cada build** — resuelto renombrando a `postcss.config.mjs`
  - El item pedia `"type": "module"` en `package.json`, pero eso arrastra bastante mas de lo que parece: el preload **debe** seguir siendo CJS porque la ventana corre con `sandbox: true`, el main pasa a `.mjs` y pierde `__dirname`, y hay que reapuntar el campo `main`. El rename cierra el objetivo real con riesgo cero; la migracion ESM completa queda anotada en P5
- [x] 🔍 **Añadir ESLint** — resuelto. `eslint.config.mjs` (flat config) mas `pnpm lint`; el repo queda en **0 errores**
  - La indentacion **no se unifica**: el linter obliga a cada zona a mantener la que ya usa (4 en `managers`/`utils`/`windows`, 2 en `main/index.js`, `ipc`, `preload`, `shared` y el renderer). Reformatear el arbol entero enterraria el historial de ficheros que siguen en obras; unificarla toca en el barrido de `typescript-eslint` de P5
  - Dos trampas que costaron tiempo, por si hay que volver: `eslint-plugin-react` con `version: 'detect'` **revienta la ejecucion entera** en ESLint 10 (busca una API de contexto de la 9), por eso la version va fijada; y `@stylistic/indent` reformatea los literales de plantilla con clases de Tailwind dejandolos ilegibles, por eso van en `ignoredNodes`
  - `react-refresh/only-export-components` queda apagada: los cuatro contexts exportan a proposito el provider y su hook desde el mismo fichero
  - Queda **1 warning real** sin tocar: `DynamicBackground.jsx:16` tiene un `useEffect` al que le falta `currentBg` en las dependencias. Es señal, no ruido, y se mira aparte — recogido ahora en el item de disciplina de `useEffect`, mas abajo

- [x] **Las tipografias no se aplicaban — dos causas independientes que se tapaban** — resuelto. El sintoma visible era un error de CSP en consola, pero arreglar solo eso no habria cambiado nada en pantalla
  - **Causa A**: `App.css` abria con un `@import url('https://fonts.googleapis.com/...')`. Al ser URL absoluta vite **no lo inlinea**, lo deja en el CSS compilado y el navegador lo pide en runtime, donde `style-src 'self' 'unsafe-inline'` lo bloquea. Inter pasa a ir **auto-alojada** con `@fontsource-variable/inter`: vite emite los woff2 como assets locales, que `font-src 'self'` ya permite, asi que **no hubo que aflojar la CSP** que P1 endurecio
  - **Causa B, que no reportaba nadie**: el `@theme` declaraba `--font-family-inter`, pero en Tailwind v4 el namespace de familia tipografica es `--font-*`. Un token en un namespace inexistente **no falla, simplemente no genera nada**: `.font-inter` tenia **0 ocurrencias** en el bundle pese a usarse en 10 sitios, y toda la app llevaba desde siempre renderizando con el `--font-sans` por defecto. Comparar con `--color-accent-green` → `.bg-accent-green`, que si estaba, fue lo que lo delato
  - La familia que registra el paquete es `'Inter Variable'`, no `'Inter'`; escribir la equivocada reproduce el bug en silencio
  - Montserrat eliminada: `font-montserrat` no se usaba en ningun sitio de `src/`, solo se descargaba
- [ ] **Disciplina de `useEffect` — queda 1 efecto mal escrito** — la regla queda fijada en [ADR-0008](docs/adr/0008-useeffect-only-for-external-systems.md) y en `CLAUDE.md`: un efecto sincroniza con un sistema **externo** a React y nada mas, y el que se quede necesita cleanup, cancelacion, dependencias honestas y ruta de error. La auditoria del 01/09/2026 encontro 8 efectos, **ninguno del tipo anti-patron** (no hay estado derivado ni props copiadas a estado), y cuatro incumplian la lista. Tres se cerraron en el barrido de P4, porque tocaban los mismos ficheros:
  - `DynamicBackground.jsx:8` (el warning de ESLint apunta a la lista de dependencias, `:16`) — **pendiente**, y es peor de lo anotado alli: lee `currentBg` pero declara `[bannerUrl]`, **y no tiene cleanup**, asi que dos cambios rapidos de modpack dejan que un `img.onload` tardio pise el fondo correcto. Se cierra con un flag `cancelled` en el cleanup. Es el unico warning que queda en `pnpm lint`
  - ~~`SettingsScreen.jsx:10` sin `.catch`~~ — resuelto con el item de RAM de P4: la pantalla ya no se queda en "Cargando..." para siempre; hay estado de fallo, boton "Volver" y aviso por `useStatus()`
  - ~~`useServers.js:11` sin cancelacion~~ — resuelto con el item de errores de Firestore de P4: flag `ignore` en el cleanup, y las banderas de carga se resetean en el handler de "Reintentar" y no en el cuerpo del efecto (la regla `react-hooks/set-state-in-effect` lo prohibe)
  - ~~`LogViewer.jsx:16` autoscroll forzado~~ — resuelto con el modal de logs de P4: salta al fondo al abrir y despues solo sigue la cola si el lector ya estaba abajo
  - **Opcional, ya justificado**: `AuthContext` y `SettingsScreen` reimplementan cada uno su maquina loading/error, y las dos incompletas. Con `useServers` ya reescrito quedan dos llamantes, asi que un hook comun (`useIpcResource`) esta ahora mas cerca del limite de YAGNI que antes — revisar si aparece un tercero. Descartada una libreria de fetching: el renderer ya carga ~1.2 MB de Firebase y P5 lo quiere **mas pequeño**
  - De paso, `AuthContext.jsx:10` mezcla dos cosas en un efecto (carga inicial + suscripcion a `onTokenExpired`); separarlas desacopla la suscripcion del ciclo de la peticion. Y en `LaunchContext.jsx:15` el `cleanupRef` sobra: una `const` local dentro del efecto hace lo mismo

## P4 — Funcionalidades faltantes

De los 15 items, **10 cerrados** en la rama `feat/p4-resolve-features-from-todo`. Los cinco que quedan estan abajo, cada uno con lo que le falta para poder empezarse.

- [x] **Boton matar juego** — resuelto. Boton rojo con una X pegado al borde derecho del boton de jugar, visible solo con el juego corriendo, con modal de confirmacion porque matar el proceso pierde el progreso no guardado
  - **Encontrado de paso**: `killGame()` anulaba `this.gameProcess` justo despues de `kill()`, asi que el guard de `config:setDataDirectory` creia que no habia juego mientras la JVM aun agonizaba con ficheros abiertos en la instancia. Ahora el handler `close` es el unico que anula la referencia
  - No se podia separar del item de serializar: con dos lanzamientos concurrentes el segundo pisaba `this.gameProcess` y la X habria matado el proceso equivocado
- [ ] **Cancelar descarga/lanzamiento** — una vez se pulsa JUGAR no hay forma de cancelar. Ni `DistributionManager.applySync` ni `downloadQueue` de helios aceptan señal de aborto, asi que es trabajo de main, no de UI: hace falta un `AbortController` que atraviese la cadena entera
- [x] **Errores de Firestore visibles** — resuelto junto con el modo offline: era el mismo problema. `useServers` ya capturaba el error y lo guardaba, pero nadie lo pintaba, asi que la sidebar caia en "Sin servidores", indistinguible de "no tienes modpacks asignados"
- [x] **Modo offline** — resuelto, pero **no como se planteaba**: el launcher es online-only y no puede dejar de serlo ([ADR-0009](docs/adr/0009-no-offline-mode.md)). Lanzar exige un token de Minecraft valido, que sale de la cadena Microsoft → XBL → XSTS, toda ella de red, y caduca en ~24 h. Cachear la lista solo enseñaria modpacks que no se pueden jugar. Lo que se hace es fallar claro: panel de error con boton "Reintentar" y "Sin conexion" en la sidebar
  - **Trampa encontrada**: con el SDK completo de Firebase, `getDocs` sin red **no rechaza, reintenta indefinidamente**, asi que el sintoma real no era un error sino un "Cargando..." infinito. Las lecturas van ahora contra un timeout de 12 s. El paso a `firebase/firestore/lite` de P5 lo resolveria de raiz
- [x] **RAM en settings** — resuelto con el diseño acordado: toggle **"Usar RAM asignada a cada modpack (Recomendado)"** (`java.useModpackRam`, default `true`) mas un slider en MB ligado a un input. `LaunchManager` deja de dar la victoria automatica al valor del modpack
  - El tope es el **75 % de `os.totalmem()`**, tope duro y no aviso: una JVM que se queda con toda la RAM fisica deja al SO sin nada y acaba en swapping o en un cierre del juego
  - **No estaba en el diseño y hacia falta**: `setMaxRAM` tiene que arrastrar `minRAM` hacia abajo. Con los defaults `2G`/`4G`, bajar el slider a 1024 MB dejaba `-Xms2G -Xmx1G` y la JVM **no arranca**. Lo mismo se clampa en `buildLaunchCommand`, porque el `java.minRam`/`maxRam` del modpack tampoco lo valida nadie
  - El formato en disco sigue siendo un string; el slider escribe `'${mb}M'`, que la JVM acepta igual que `G`. MB es la unica unidad que cruza el IPC
- [ ] **Segunda barra: progreso de arranque de Minecraft** — hoy el feedback termina al hacer `spawn`, asi que el usuario se queda mirando una ventana quieta mientras la JVM arranca. Añadir una segunda barra alimentada por hitos del stdout del juego
  - **El bloqueante que tenia ya esta resuelto.** El item pedia comprobar si `javaw.exe`, que no tiene consola, entrega stdout. Medido el 01/09/2026 con `child_process.spawn` y stdio `pipe`: `javaw.exe` y `java.exe` devuelven **exactamente los mismos 5575 bytes** por stdout con `--help`. El problema de `javaw` es la asignacion de consola cuando el stdio se **hereda**, no los pipes. **No hace falta** el truco de `ModLoaderManager._consoleJavaExec` ni abrir una consola junto al juego
  - Lo que sigue siendo trabajo: un buffer por lineas **antes** del `.trim()` (`LaunchManager.js` reenvia los chunks tal cual y un chunk puede partir una linea a la mitad), una tabla ordenada de hitos, un tipo de progreso nuevo con su `case` en `LaunchContext` y su tramo en `launchPhases.js`, y un latch que se resetee junto a `this.gameProcess = null`
  - Sigue siendo **heuristico**: el formato de log varia por loader, version y mods, asi que la barra debe degradar a indeterminada cuando no reconozca ningun hito, nunca quedarse colgada. La tabla solo se calibra de verdad lanzando un modpack real. Ojo tambien: `{type:'started'}` se emite justo despues de `spawn`, no cuando la ventana abre
- [x] **Logs en modal centrado con efecto glass** — resuelto. Deja de ser un panel en linea de `max-h-[180px]` y pasa a un modal centrado
  - No existia ningun modal, portal ni manejo de Escape en el renderer: `components/common/Modal.jsx` es nuevo y lo comparten los logs y la confirmacion de matar el juego. Reutiliza la receta de cristal y el patron de click-fuera de `UserProfile.jsx`
  - **Decision de diseño resuelta**: se conserva el color por tipo (verde info, amarillo warn, rojo error, azul system). Pintarlo todo verde borraria justo la distincion que el usuario necesita cuando abre los logs porque algo ha fallado
- [x] **Contador de logs "+99"** — resuelto, a partir de 100 el disparador muestra `+99`. El buffer sigue capado a 500 en `LaunchContext`
- [x] **Pantalla completa por defecto** — resuelto, `fullscreen: true` en `getDefaultConfig()`. Solo afecta a instalaciones nuevas: `validateConfig` rellena claves **ausentes**, asi que un `config.json` existente conserva su valor
- [x] **Boton "Restablecer" en el directorio de datos** — resuelto, junto al de "Buscar" y habilitado solo cuando la ruta efectiva difiere de `defaultDataDirectory`. Llama a `setDataDirectory(null)`
- [ ] **Exportar logs** — se muestran en la UI (max 500 lineas) pero no se pueden copiar ni exportar. Con el modal ya montado, el sitio natural es un boton en su cabecera
- [ ] **Detalles del modpack** — no se ve la lista de mods ni la version del loader. **Falta definicion** de que se enseña y donde; el dato existe en el manifest, que ya se descarga y se cachea en `manifests/<sha256>.json`
- [ ] **Barra de titulo custom** — la ventana usa el frame nativo, rompe con el diseño glassmorphism. Los tres canales (`window:minimize/maximize/close`) ya existen en las 4 capas y `registerWindowIPC` ya hace el toggle de maximizar; falta `frame: false`, el componente con `-webkit-app-region: drag` y ajustar el alto de las tres pantallas, que hoy son todas `h-screen`. El boton maximizar/restaurar necesita ademas conocer el estado para cambiar de icono
- [x] **Token refresh por calculo** — resuelto. El `setInterval` de 5 min desaparece: ahora es un `setTimeout` calculado desde `expiresAt` menos 5 min, acotado a `[15 s, 30 min]`, con backoff `[1, 2, 5, 10 min]` en fallo recuperable y aviso al renderer **solo** si el fallo es terminal
  - El comportamiento anterior no era solo "poll": `monitorTokenExpiration` salia de inmediato si `expiresAt > now`, asi que **nunca refrescaba antes de expirar**, solo despues
  - El tope de 30 min es un latido: evita que un `expiresAt` corrupto deje al scheduler dormido para siempre
  - Se engancha `powerMonitor.on('resume')`, porque un `setTimeout` no corre durante la suspension y dispara tarde al despertar — el caso "cierro la tapa por la noche"
  - **Encontrado de paso**: `registerAllIPC` corre dentro de `createWindow()`, que macOS vuelve a llamar en `activate`. Eso duplicaba el `setInterval` sin limpiarlo nunca **y hacia lanzar a `ipcMain.handle` por handler duplicado**. `handle()` hace ahora `removeHandler` antes de registrar, y arrancar el scheduler es idempotente
- [x] **Serializar el lanzamiento** — resuelto con un latch `launchInProgress` en `LaunchManager` mas la comprobacion de `gameProcess != null`, y un `LAUNCH_ALREADY_RUNNING` nuevo
  - El guard va **antes** del `try` y no pasa por el `progressCallback`: emitir `{type:'error'}` habria mandado a `LaunchContext` al estado de error y roto la UI del lanzamiento que si estaba corriendo
  - Era alcanzable de verdad, no teorico: en `pnpm dev` un F5 del renderer resetea `launchState` a `idle` con el juego abierto, y JUGAR volvia a estar disponible
  - Va con un canal `launch:getStatus` nuevo que `LaunchContext` consulta al montar; sin el, tras un reload el renderer no sabe que hay juego corriendo y el boton matar no aparece

## P5 — Mantenimiento y evolucion

- [x] **`helios-distribution-types` faltaba y el launcher no arrancaba desde cero** — resuelto, devuelta a `dependencies`. Se quito en `9f8b112` por "no usada", y lo parecia: nada en `src/` la importa. La necesita `helios-core/common` en runtime (→ `DistributionAPI` → `DistributionFactory`), que es de donde `AuthManager` saca `RestResponseStatus`, y helios-core solo la declara como **devDependency**, asi que la tiene que poner el consumidor
  - Estuvo oculto desde el 14/08 porque el `node_modules` existente seguia teniendo el paquete en disco; solo aparece en una instalacion limpia
  - Añadido tambien `pnpm.onlyBuiltDependencies`: pnpm 10 ignora los scripts de postinstall salvo que el paquete este en esa lista, y sin `electron` ahi no se descarga el binario y `pnpm dev` no arranca

- [ ] 🔍 **Electron 37 → 43** — la 37 esta fuera de soporte; se acumulan CVEs de Chromium
- [ ] 🔍 **`firebase/firestore/lite`** — solo se usan `getDoc`/`getDocs`; el bundle del renderer son ~1.2 MB casi todos de Firebase
- [ ] 🔍 **Actualizaciones menores** — react 19.2.8, firebase 12.17, electron-builder 26.15
- [ ] **Soporte de NeoForge** — su instalador es un fork del de Forge y acepta el mismo `--installClient`, asi que `ModLoaderManager.installForge` sirve tal cual; solo cambia el host del maven (`maven.neoforged.net`). El riesgo esta en el id de version, que no es uniforme: `neoforge-21.1.72` en MC 1.20.2+ pero `1.20.1-forge-47.1.106` en 1.20.1. Leerlo del `version.json` que hay dentro del jar del instalador (AdmZip ya es dependencia) en vez de componerlo con una plantilla. Requiere antes cerrar el soporte de Forge — ver "Pending" en `docs/manifest.md`
- [ ] **`"type": "module"` / migracion a ESM** — sacado del P3, donde su unico objetivo real (el warning de postcss) ya esta cerrado por el rename a `.mjs`. Hacerlo de verdad implica: el preload **sigue obligatoriamente en CJS** por `sandbox: true`, el main pasa a `.mjs` y hay que sustituir `__dirname` por `import.meta`, y reapuntar el campo `main` de `package.json`. Se prueba con `pnpm package`, no con `pnpm dev`. Va con la migracion a TypeScript, que revisa la salida de build igualmente
- [ ] **Migracion a TypeScript** — incremental con `allowJs`, nunca de golpe: cada paso debe ser un commit que compila y que no congela el resto del TODO. Deps: `typescript`, `@types/node`, `@types/react`, `@types/react-dom`; `helios-core` ya trae sus `.d.ts` (`node_modules/helios-core/dist/**/*.d.ts`)
  1. `tsconfig.json` por proceso (electron-vite lo soporta) con `allowJs: true`, `checkJs` activado por carpetas a medida que se migra, y `strict: true` para todo `.ts` nuevo
  2. **Tipar primero los contratos que hoy fallan en silencio**, que es donde esta casi todo el retorno:
     - Canales IPC: un mapa `canal → (args, retorno)` hace que la regla de las 4 capas de `CLAUDE.md` (channels → handler → preload → ipcClient) la verifique el compilador en vez de la revision manual, y los canales muertos como el `config:load` que se borro en el P1 dejan de poder existir
     - El evento `launch:progress` como **union discriminada** por `type`: un `type` nuevo sin su `case` en `LaunchContext` pasa a ser un error de compilacion en vez de una UI congelada en el mensaje anterior
     - El esquema del manifest (`docs/manifest.md`) y la forma del `config.json`, incluido el sello `CONFIG_VERSION`
  3. Migrar los managers de uno en uno, main primero (no hay JSX) y de hoja a raiz: `utils/` → `ConfigManager` → `ManifestManager`/`DistributionManager` → `ModLoaderManager`/`LaunchManager` → `AuthManager`
  4. Renderer al final: contexts → hooks → components
  5. Se hace junto con la entrada de ESLint de P3 (`typescript-eslint`), que de paso cierra la divergencia de indentacion 4 vs 2 espacios en un solo barrido
- [ ] **Auto-update** (electron-updater) — no existe hoy; es lo esperable en un launcher
- [ ] **Firma de codigo en Windows** — sin ella el instalador y el portable disparan SmartScreen
